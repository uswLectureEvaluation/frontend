import styled from 'styled-components'
import { Container } from '../globalStyles'

export const HeadSelection = styled(Container)`
display: flex;
//아이템들의 사이에 균일한 간격을 만들어준다
justify-content: space-between;
height: 48px;

`